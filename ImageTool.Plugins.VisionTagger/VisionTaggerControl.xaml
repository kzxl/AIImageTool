<UserControl x:Class="ImageTool.Plugins.VisionTagger.VisionTaggerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Foreground="White"
             Background="#1E1E1E">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Trái: Khung hiển thị hình ảnh -->
        <Border Grid.Column="0" Background="#252526" BorderBrush="#333333" BorderThickness="1" CornerRadius="8">
            <Grid>
                <TextBlock x:Name="tbPlaceholder" Text="Kéo thả hoặc dán ảnh vào đây" Foreground="#888888" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" IsHitTestVisible="False" Panel.ZIndex="1"/>
                <Image x:Name="imgPreview" Stretch="Uniform" Margin="10"/>
                
                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Button x:Name="btnLoadImage" Content="Tải Ảnh Lên" Width="150" Height="35" Style="{DynamicResource RoundedButtonStyle}" Click="BtnLoadImage_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

        <!-- Phải: Kết quả phân tích -->
        <Grid Grid.Column="2" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Tiêu đề mô tả -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" >
                <TextBlock Text="Mô Tả Ảnh" FontSize="16" FontWeight="SemiBold"/>
                <Button x:Name="btnCopyDesc" Content="Copy" Margin="10,0,0,0" Padding="5,2" Click="BtnCopyDesc_Click"/>
            </StackPanel>
            
            <!-- Mô tả -->
            <TextBox x:Name="txtDescription" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" 
                     MinHeight="100" Padding="10" Background="#2D2D30" Foreground="White" BorderThickness="1" BorderBrush="#3E3E42" />

            <!-- Nút Action -->
            <Button x:Name="btnAnalyze" Grid.Row="3" Content="Phân Tích Bằng AI" Height="40" Margin="0,15,0,15" Click="BtnAnalyze_Click" FontWeight="Bold"/>

            <!-- Tiêu đề Tags -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Từ Khoá (Tags)" FontSize="16" FontWeight="SemiBold"/>
                <Button x:Name="btnCopyTags" Content="Copy All" Margin="10,0,0,0" Padding="5,2" Click="BtnCopyTags_Click"/>
            </StackPanel>
            
            <!-- Area chứa tags -->
            <ScrollViewer Grid.Row="5" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="lstTags">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#007ACC" CornerRadius="4" Padding="8,4" Margin="3">
                                <TextBlock Text="{Binding}" Foreground="White"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            
            <!-- Loading Indicator -->
            <StackPanel x:Name="pnlLoading" Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Visibility="Collapsed" Margin="0,10,0,0">
                <ProgressBar IsIndeterminate="True" Width="20" Height="20" Style="{DynamicResource CircularProgressBarStyle}"/>
                <TextBlock Text="Đang xử lý AI..." Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#AAAAAA"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
