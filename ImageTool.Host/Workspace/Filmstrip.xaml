<UserControl x:Class="ImageTool.Host.Workspace.Filmstrip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BgBaseBrush}" Height="110">
    <Grid>
        <ScrollViewer x:Name="scroller" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="icStrip">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="90" Height="90" Margin="3" Background="{DynamicResource BgPanelAltBrush}"
                                BorderThickness="2" CornerRadius="2" Cursor="Hand"
                                MouseLeftButtonUp="StripItem_MouseLeftButtonUp"
                                MouseRightButtonUp="StripItem_MouseRightButtonUp">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush_}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentWarmBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Image Source="{Binding Thumb}" Stretch="Uniform"/>
                                <Border Width="8" Height="8" CornerRadius="4"
                                        HorizontalAlignment="Left" VerticalAlignment="Top" Margin="3"
                                        Background="{Binding LabelBrush}"/>
                                <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="3"
                                        Background="#CC55C1E7" CornerRadius="2" Padding="2,0"
                                        Visibility="{Binding IsEdited, Converter={StaticResource BoolToVis}}"
                                        ToolTip="Đã chỉnh sửa">
                                    <TextBlock Text="✎" Foreground="#000" FontSize="8" FontWeight="Bold"/>
                                </Border>
                                <TextBlock Text="{Binding RatingDisplay}" Foreground="#FFD24E" FontSize="9"
                                           HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,3,2"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
